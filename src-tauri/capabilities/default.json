{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "fs:allow-document-read-recursive",
    "fs:allow-document-write-recursive",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://api.nlt.to/**" }]
    }
  ]
}
